#ifndef RESULT_H
#define RESULT_H

typedef struct{	
	int row;
	int col;
	char **cArrayCopy;
} Result; /* struct with variables for storing all the map row value, map column value, and map array copy for the log file */

#endif 
